import { MyListingsView } from "@/components/my-listings/listings-view";

export const metadata = { title: "My Listings — Snap'n'Swap" };

export default function MyListingsPage() {
  return <MyListingsView />;
}
