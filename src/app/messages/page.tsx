import { MessagesView } from "@/components/messages/messages-view";

export const metadata = { title: "Swap Requests — Snap'n'Swap" };

export default function MessagesPage() {
  return <MessagesView />;
}
