import { SwapsView } from "@/components/swaps/swaps-view";

export const metadata = {
  title: "My Swaps — Snap'n'Swap",
  description: "Track your active swap proposals and history.",
};

export default function SwapsPage() {
  return <SwapsView />;
}
