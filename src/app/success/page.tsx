import { SuccessView } from "@/components/success-view";

export const metadata = {
  title: "Purchase Complete — Snap'n'Swap",
};

export default function SuccessPage() {
  return <SuccessView />;
}
